import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: 'gray',
    width: '100%',
    backgroundColor: 'white',
  },
  container: {
    height: 40,
    width: '100%',

    margin: 1,
  },
});
